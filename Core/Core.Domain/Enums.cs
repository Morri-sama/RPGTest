using System;
using System.Collections.Generic;
using System.Text;

namespace Core.Domain
{
    public class Enums
    {
        public enum AttackType { Melee = 0, Range = 1, Magical = 2 }
        public enum DamageType { Physical = 0, Magical = 1 }
    }
}
